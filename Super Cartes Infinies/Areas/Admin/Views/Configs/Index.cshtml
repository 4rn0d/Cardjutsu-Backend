@model IEnumerable<Super_Cartes_Infinies.Models.Config>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.NbCardsStart)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.ManaPerRound)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.NbDecks)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.NbCarteParDeck)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.NbCardsStart)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.ManaPerRound)
            </td>
                <td>
                    @Html.DisplayFor(modelItem => item.NbDecks)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.NbCarteParDeck)
                </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.Id">Edit</a> |
                <a asp-action="Delete" asp-route-id="@item.Id">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
